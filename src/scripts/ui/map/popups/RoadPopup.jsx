var React = require('react');
var M = require('morearty');

var RoadPopup = React.createClass({
  mixins: [M.Mixin],

  render () {
    return (
      <div />
    );
  }
});

module.exports = RoadPopup;
